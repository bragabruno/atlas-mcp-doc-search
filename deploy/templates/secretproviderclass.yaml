{{- /*
SecretProviderClass — maps Azure Key Vault secrets (ES API key, Qdrant API key,
gateway API key) onto the CSI volume mounted by the pod (atlas-docs/04 §3).
keyVaultName / tenantId / identity client-id are injected per env, never committed.
*/ -}}
{{- if .Values.secrets.enabled }}
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ include "atlas-mcp-doc-search.fullname" . }}
  labels:
    {{- include "atlas-mcp-doc-search.labels" . | nindent 4 }}
spec:
  provider: azure
  parameters:
    usePodIdentity: "false"
    useVMManagedIdentity: "false"
    clientID: {{ .Values.secrets.userAssignedIdentityID | quote }}
    keyvaultName: {{ required "secrets.keyVaultName is required when secrets.enabled" .Values.secrets.keyVaultName | quote }}
    tenantId: {{ required "secrets.tenantId is required when secrets.enabled" .Values.secrets.tenantId | quote }}
    objects: |
      array:
        {{- range .Values.secrets.objects }}
        - |
          objectName: {{ .objectName }}
          objectType: {{ .objectType }}
        {{- end }}
{{- end }}
